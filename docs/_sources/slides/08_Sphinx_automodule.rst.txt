Autodoc extension
********************

It allows to extract documentation from module, class, function.
This is flexible and powerful system. But sometimes require a time to get result you want.
To get good documentation you need to have good documentation in source code.

Example of automodule directive
===============================

.. automodule:: module_1
    :members:
    :show-inheritance:

..
  Модуль автодок позволяет доставать документацию из модулей, классов, функций.
  Это достаточно гибкая и мощная система позволяющая настаивать полученный результат.
  Но иногда приходится попотеть, чтобы добиться того, что хочется.
  Иногда это в принципе не возможно, пример этого документация для языка питон, где все написано вручную,
  титаническая работа, но это одно из редких мест, где она оправдана.